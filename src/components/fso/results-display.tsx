
"use client";

// This component is being phased out. Its content has been integrated into BottomPanel.tsx's stats header.
// Keeping the file temporarily to avoid breaking any potential missed imports during refactor,
// but it should ideally be removed in a subsequent cleanup.

export default function ResultsDisplay() {
  // console.warn("ResultsDisplay.tsx is deprecated and should be removed. Its logic moved to BottomPanel.tsx.");
  return null; 
}
